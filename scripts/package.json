{
  "name": "guess-the-song-scripts",
  "version": "1.0.0",
  "description": "Deployment/dev-environment shell scripts. Preprocessing now happens via getting_the_data/kaggle_level_pipeline.ipynb + build_preprocessed_folders.py, and seeding via backend's own `npm run seed` (backend/src/scripts/seed.ts) - see setup.sh."
}

